---
// TODO: Replace with your actual highlights
const highlights = [
  {
    icon: "briefcase",
    title: "Internship",
    description: "TODO: Software Engineering Intern at Company",
  },
  {
    icon: "trophy",
    title: "Award",
    description: "TODO: Hackathon Winner / Dean's List / etc.",
  },
  {
    icon: "users",
    title: "Leadership",
    description: "TODO: President of CS Club / Team Lead / etc.",
  },
  {
    icon: "code",
    title: "Key Tech",
    description: "TODO: Full-stack, ML, Cloud — your strongest area",
  },
];
---

<section class="py-12 animate-on-scroll">
  <h2 class="text-2xl font-bold mb-6">Highlights</h2>
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
    {highlights.map((item) => (
      <div class="card bg-base-200 card-hover p-5">
        <div class="flex items-start gap-3">
          <div class="w-10 h-10 rounded-lg bg-primary/10 flex items-center justify-center flex-shrink-0">
            {item.icon === "briefcase" && (
              <svg class="w-5 h-5 text-primary" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" /></svg>
            )}
            {item.icon === "trophy" && (
              <svg class="w-5 h-5 text-primary" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M5 3h14M9 3v2a4 4 0 004 0V3M12 11v4m-4 4h8m-4-4v4M7 7a5 5 0 005 5 5 5 0 005-5" /></svg>
            )}
            {item.icon === "users" && (
              <svg class="w-5 h-5 text-primary" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z" /></svg>
            )}
            {item.icon === "code" && (
              <svg class="w-5 h-5 text-primary" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4" /></svg>
            )}
          </div>
          <div>
            <h3 class="font-semibold text-sm">{item.title}</h3>
            <p class="text-sm text-base-content/60 mt-1">{item.description}</p>
          </div>
        </div>
      </div>
    ))}
  </div>
</section>
